name: Monitor private league health
on:
  schedule:
    - cron: '7,22,37,52 * * * *'
  push:
    branches: [main]
    paths: ['.github/workflows/health.yml', 'app/scripts/check-health.mjs']
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: gridiron-edge-health
  cancel-in-progress: false
jobs:
  health:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Check private sync and scheduler
        env:
          OWNER_ACCESS_KEY: ${{ secrets.OWNER_ACCESS_KEY }}
        run: node app/scripts/check-health.mjs
